@model IEnumerable<BoxingList.Models.Boxer>
@{
    ViewData["Title"] = "Index";
}

<br />
<h3>Boxer List</h3>

<hr />
<p>
    <a asp-action = "Create" class=" btn btn-sm btn-primary">Create New</a>
</p>

@if (Model.Count() > 0)
{
    //Display all boxers
}
else
{
    <div>No Boxers Exists... Please Add a new boxer.</div>
}

